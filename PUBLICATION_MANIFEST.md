# GitHub 发布清单

## 发布目的

本目录是面向竞赛评委的轻量、可运行、可追溯版本。它从完整本地研究工作区中
筛选核心代码、许可明确的数据、聚合结果和说明材料，避免把数十 GB 原始数据、
重复快照、临时文件或许可不清内容推送到 GitHub。

## 已包含

- LifeTwin Python 源代码及 Phase 6-8、landmark、V4 和外部应力 runner。
- 冻结实验配置、前缀推理 Schema、长期验证 Schema/模板和阶段技术报告。
- CC BY 4.0 的 Naumann 规范化条件均值表与 Geisbauer 原始 LFP CSV。
- Phase 8 聚合比较、tau 敏感性和目标诊断表。
- 动态 landmark、V4 校准/拒绝和 Geisbauer 外部负结果的机器可读证据包。
- V4 全部 210 个校准划分、Geisbauer 逐电芯/LOCO 稳健性证据和审计总图。
- 长期 LFP 数据集资格登记、数据无关预注册 Schema/模板、跨字段语义验证器和机器可读禁止宣称。
- 公开版复现测试、GitHub Actions 和图表生成脚本。
- 不含未来容量的前缀请求、可调用 API/CLI 和自包含中文评审控制台。
- V0.14 预注册合成长时域压力测试的冻结协议、紧凑证据、完整性验证器、结果报告和总图。
- 开题补充材料、相关项目经验、数据分析样本、研究笔记和参考清单。

## 主动排除

| 排除内容 | 原因 |
|---|---|
| FastCharge、CellJAR 原始/处理后大文件 | 文件规模大，且应由上游来源和固定版本重新获取 |
| 大型 Phase 6-8 全量预测包和重复运行快照 | GitHub 评审不需要重复数 GB 证据；V0.11 的小型预测包和 V0.14 紧凑证据保留用于直接审计 |
| V0.14 的 112 MB 完整 24 文件运行目录 | 不放入 Git 历史；以带 SHA-256 的确定性 ZIP 作为 GitHub Release 资产发布 |
| Lam/Joule 数据样本和作者代码 | 数据和软件许可仍待澄清 |
| Vachenauer、Yagci、Sui 与 Aeppli 请求型数据 | 只登记一手来源和资格；未获得明确数据许可，不复制或分发 |
| 许可请求邮件与发送截图 | 不属于评委复现实验所需材料 |
| 非 canonical 和失败前临时产物 | 防止评委误用旧结果 |
| pytest 临时目录、缓存和本地绝对路径 | 环境噪声与隐私控制 |
| 海辰内部数据 | 本项目未获得，也未虚构 |

## 发布验证

机器可读哈希位于 [`release_manifest.json`](release_manifest.json)，运行：

```powershell
.\.venv\Scripts\python.exe scripts\verify_public_release.py
.\.venv\Scripts\python.exe -m pytest -q
```

发布版的目标是“评委可理解、核心结果可复算、边界可检查”，不是完整复制本地
研究归档。

## Phase 1 审计增量

`v0.10.0` 在原 Phase 8 结果之外新增对抗性审计代码、机器可读审计产物、
失败条件表、评分完整性测试、门控故障注入测试和跨平台一键复现入口。发布清单
同时冻结这些证据文件的 SHA-256。被 Git 忽略的 `artifacts/` 仅用于本地或 CI
重跑结果，不属于 GitHub 发布内容，也不会覆盖已发布证据。

fresh clone 使用 Python 3.12.x，并以 `requirements/reproduction.txt` 约束安装依赖后使用：

```powershell
.\.venv\Scripts\python.exe scripts\reproduce_public_release.py --mode full --output artifacts\reproduction
```

Linux/macOS：

```bash
.venv/bin/python scripts/reproduce_public_release.py --mode full --output artifacts/reproduction
```

命令先验证 Git 跟踪文件和冻结哈希，再运行 Phase 8、Phase 1 审计、无界面绘图与
完整测试；失败时不发布半成品输出目录。
清单所列的发布证据文件必须精确匹配 SHA-256。Phase 8 核心表跨平台重算使用 `2e-4`
绝对容差；Phase 1 表按文件声明主键和字段策略核对，身份/协议/计数/真值/状态字段精确
匹配，求解器派生误差指标限于 `5e-3 pp`，派生比例限于 `1e-4`，审计残差限于
`1e-10`，并拒绝非有限值。派生模型状态哈希验证格式、行间等价类结构与同运行内部一致性。
独立重算残差、消融关系、回退关系、排名和风险标签还要满足同一行及同组内的不变量。

## V0.11 证据增量

`v0.11.0` 新增三条彼此独立的证据链：固定共同未来窗口的动态 landmark 诊断；
7/6/4 条件切分下的“机理层级均值 + LOCO 有界残差 + 路由化校准”；以及许可明确的
Geisbauer 15 电芯、120 天、60 C 外部应力筛查。后者的主候选没有优于目标前缀
平方根比较器，负结果与逐电芯指标一并发布。

一键复现会重新运行三项实验并核对冻结结果、核心 CSV 和禁止宣称。当前仍明确禁止：
把 `p=10` 称为确认性最早 landmark；把回顾性诊断区间称为正式覆盖保证；把 120 天
高温筛查称为长期外部验证；以及任何海辰产品、真实电站或 15-25 年精度声明。

## V0.12 稳健性与预注册增量

`v0.12.0` 固定 v0.11 的均值模型和训练状态，枚举 V4 的全部 210 个重叠
`6 calibration + 4 evaluation` 划分，并对 Geisbauer 15 个物理电芯执行配对、
SOC/时间分层、exact nominal diagnostics 和 leave-one-cell-out。结果显示 fallback
80% 区间宽度依赖校准条件，specialist 结构性欠校准；Geisbauer 平均负迁移很小且
LOCO 可改变方向。两项审计都明确是查看旧结果后设计的回顾性诊断。

发布版同时冻结长期 LFP 数据资格登记、数据集无关的预注册 Schema/模板和生产语义验证器。数据许可、
物理电芯 ID、LFP/石墨体系、可分离日历老化、时长、结局接触史和证据等级必须在读取
目标值前记录；语义验证器会复算分区互斥、实际计数和三项强制基线，不能只填写“已验证”。
当前合格公开长期确认集为 0；十年端点、混合 Second-Life 或系统现场
数据不能被重新命名为动态 landmark 确认。

一键复现新增两个 v0.12 runner，并将生成 JSON/CSV 与发布证据作跨平台语义核对。
JSON 中平台易变的预测状态哈希只验证结构和同运行一致性，不要求不同 BLAS/CPU 的末位
字节相同；数据结果哈希、结论字段与禁止宣称仍严格核对。

`release_manifest.json` 的 v0.12 严格门禁还要求除清单自身外的每一个 Git tracked
常规文件都具有冻结 SHA-256；文件只被提交但漏出哈希表时，验证会报告
`unfrozen_tracked_files` 并失败。这样新增脚本、说明或测试不能绕过发布冻结。

## V0.13 可操作入口增量

`v0.13.0` 不在已查看的 Naumann outcome 上晋升新均值模型，而是把冻结的 V4 数值路径
提升为真实的单目标前缀入口。请求必须通过关闭额外字段的 JSON Schema：前 10 点允许
容量保持率，未来 25 点只允许时间坐标；V1 锁定已经校准的 `p=10` 网格。训练参考状态
在任何结果校验前隔离四个测试条件；这些条件的未来结果即使全部遮蔽，也不会改变
参考状态或 API 输出。域外、缺少局部工况切片支持、前缀偏离联合参考轨迹或目标拟合失败的请求只返回
空数值和机器可读拒绝原因。

CLI 原子写入 `forecast.csv` 与 `decision.json`，记录请求、配置、参考数据、训练、校准、
预测和输出哈希；不覆盖已有证据目录。fallback 示例可给出 80% 回顾诊断区间，specialist
示例因同路由校准不足不提供区间，二者运营上下界都为 `null`。

发布版同时新增零安装 Judge Console。它只读取八个冻结 evidence 文件，回放通用回退、
专用路由和 Geisbauer 外部负迁移三个案例；不请求网络、不执行伪实时推理，也不补造冻结
包中缺少的前缀 SOH。测试覆盖 future 字段注入、15 年坐标、域外拒绝、异常前缀、
拟合故障、测试条件未来结果遮蔽、真实 CLI、原子性和控制台确定性。新增入口提高
可操作性，不提高现有证据等级。

## V0.14 预注册长时域压力测试增量

`v0.14.0` 新增冻结的合成 25 年结构可辨识性协议和实现。正式运行覆盖 2,900 条轨迹、
10,000 次随机拒绝排序和 5,000 次 cluster bootstrap；四项安全门通过且没有协议偏离，
但两个主效门槛未达到，因此机器可读状态和报告均保留 `failure`，不晋升该门控方法。

Git 中只保留约 2 MB 的紧凑证据和一次在预测承诺前中断的 void 记录。完整 24 文件、
112,323,415 字节运行目录的规范条目摘要为
`14e5ec5fc66b20d50a0c9793c81dbc740f5f9563ef8fba676b5701c62dd74138`；完整包通过
GitHub Release 资产分发，不规避仓库 5 MB 文件门禁。确定性 ZIP 为 14,468,311 字节，
SHA-256 是 `66efc005c7207f7d2718edb77ad6f1e93029ee377ede6a2cece0ec32087e57bd`。
验证器独立核对文件哈希、行数、
前缀身份、随机排序、bootstrap 和三项主终点。合成压力测试不能替代真实 LFP 长期验证，
也不支持海辰产品或 15-25 年精度宣称。
