# LifeTwin V0.20 / V2.5 checkpoint-registry protocol preregistration

Design date: 2026-08-12

Protocol: `synthetic_long_horizon_identifiability_v2_5`

Reserved one-shot attempt: `v025-formal-20260812-a1`

Status: `preregistered_post_generic_core_pre_formalization`

Machine-readable amendment: [`synthetic_long_horizon_identifiability_v2_5_amendment.json`](../configs/experiments/synthetic_long_horizon_identifiability_v2_5_amendment.json)

## Result-blind scope

This preregistration is written before any V2.5 seed is consumed, before any V2.5 generator or formal runner is called, and before any V2.5 formal root or sealed truth exists. Earlier formal attempts and their negative terminal records remain immutable and cannot be retried, resumed, reclassified or reused.

V2.5 changes only identity and the implementation contract needed to make the V0.20 checkpoint input registry a single exact-set source of truth. Center, risk and calibration checkpoint producers, consumers, truth firewalls and model-state validation must use the same immutable registry object. A missing, extra, renamed, reordered or byte-drifted input is a typed integrity failure before the next truth reveal.

## Frozen inheritance

The byte- and semantic-committed V2.4 amendment remains the scientific base. V2.5 does not change its six generating families, equations, distributions, data partitions, time grids, features, model variants, optimizers, parameter bounds, fit credibility, calibration, routing, fallback, rejection, uncertainty, endpoints, denominators, thresholds, family gates, safety gates, negative controls, bootstrap, Clopper-Pearson, permutation rules, success conditions or terminal classifications.

The only permitted differences are:

1. a new protocol, attempt, seed, generated-data and path identity;
2. the canonical V0.20 checkpoint exact-set registry;
3. typed fail-closed checkpoint errors before truth access;
4. parent, generation and truth-incapable prediction processes attesting the same immutable V2.5 identity.

No runtime protocol, seed, threshold, partition, registry or success-condition override is permitted.

## Fresh identity and one-shot rule

The thirteen roots are `202608120501` through `202608120513` in the stream order recorded in the amendment. They are mutually distinct and disjoint from every registered V2, V2.1, V2.2, V2.3 and V2.4 root. Recording them here does not consume them.

Only `v025-formal-20260812-a1` may ever be assigned. There is no a2. A formal launch requires a verified P→I→F topology, a clean frozen worktree, a matching runtime and package lock, absent and pairwise-disjoint formal roots, no attempt/process collision, and separate post-freeze authorization. A valid scored success, scored failure, scientific terminal, integrity void or unknown terminal is final and must be preserved without result-dependent repair.

## Commitment and truth order

The inherited order remains mandatory: fresh generation and whole-bundle validation → fit commitment → center/risk/calibration checkpoint chain → prediction commitment → heldout truth opening → deterministic scoring → exactly one scored or terminal registry. Truth may not be opened merely to diagnose a label-free or checkpoint error.

## Claim boundary

Any future result can only describe the frozen six-core-family synthetic 25-year protocol. It cannot establish performance on real LFP cells, Hithium products, storage stations or 15–25 year real-world prediction. Questions about stronger baselines, worst-case operating conditions, domain rejection, interval calibration, physical constraints, dynamic updating, business decisions, a single champion and same-protocol comparison with another model are post-result review items; they are not added retroactively as V2.5 success gates.
